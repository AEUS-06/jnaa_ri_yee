name: app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Para abrir enlaces externos (LinkedIn, GitHub, etc.)
  url_launcher: ^6.3.0

  # Para mostrar la cámara dentro de la app
  camera: ^0.11.0+2

  http: ^1.1.0

  path_provider: ^2.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
